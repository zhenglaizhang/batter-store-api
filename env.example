# 数据库配置
MYSQL_USERNAME=battery_user
MYSQL_PASSWORD=password
MYSQL_ADDRESS=localhost:3306
MYSQL_DATABASE=battery_recycling

# 或者使用 DATABASE_URL（如果使用完整的数据库连接字符串）
# DATABASE_URL=mysql://battery_user:password@localhost:3306/battery_recycling

# 服务器配置
SERVER_HOST=0.0.0.0
SERVER_PORT=3000

# 调试模式
DEBUG=True

# 日志级别
LOG_LEVEL=info

# ========== 微信云托管对象存储配置 ==========
# 以下配置用于连接微信云托管对象存储服务
# 参考：https://developers.weixin.qq.com/miniprogram/dev/wxcloudservice/wxcloudrun/src/development/storage/service/cos-sdk.html

# COS 存储桶所在地域
# 默认是 ap-shanghai（上海），其他地域环境对应填写
# 可选值：ap-beijing（北京）、ap-shanghai（上海）、ap-guangzhou（广州）等
# 查看所有地域：https://cloud.tencent.com/document/product/436/6224
COS_REGION=ap-shanghai

# COS 存储桶名称
# 获取方式：微信云托管控制台 -> 对象存储 -> 存储配置 -> 存储桶名称
COS_BUCKET_NAME=your_bucket_name_here

# 注意：
# 1. 临时密钥会自动通过 http://api.weixin.qq.com/_/cos/getauth 获取，无需配置 SecretId/SecretKey
# 2. 上传文件时会自动获取文件元数据，确保小程序端可以访问
# 3. 服务必须在微信云托管环境中运行才能使用此功能

